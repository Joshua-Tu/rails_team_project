<div>
  <%= @user.id %>
</div>

<div>
  <%= @user.username %>
</div>

<div>
  <%= @user.email %>
</div>

<div>
  <%= @user.location.city %>, 
  <%= @user.location.state %><br>
  <%= @user.location.country %>
</div>

<div>
  <%= @user.listings %>
</div>

<% if user_signed_in? and @user.id == current_user.id %>
  <%= current_user.full_name %>
<% else %>
  Hey, you are not logged in, so you can only see this.
<% end %>